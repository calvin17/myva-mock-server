{
  "name": "myva-mock-server",
  "version": "1.0.0",
  "description": "mock server for myva application",
  "main": "src/index.js",
  "scripts": {
    "start": "SET NODE_ENV=production && mockoon-cli start --data ./src/myva.json --port 3000",
    "dev": "SET NODE_ENV=development && mockoon-cli start --data ./src/myva.json --port 3000"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@mockoon/cli": "^8.3.0"
  }
}
